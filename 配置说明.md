# 装修防宰攻略 · 笔记版 —— 配置说明（小白详细版）

这个项目是一个 Flask 小网站：左边是导航、右边是装修攻略，每个知识点后面都能记你自己的笔记（文字 + 贴图 + 链接），笔记存在服务器上的数据库里。下面告诉你**每个文件是干嘛的、图片该放哪**、以及**想改东西改哪里**。

---

## 一、目录结构 & 每一块是干嘛的

```
renovation_notes/                ← 项目根目录（服务器上在 /usr/local/renovation_notes）
│
├── config.py          ⭐【配置文件·总开关】账号、端口、密码、图片上限都在这改
├── app.py             【后端主程序】页面注入 + 笔记接口 + 图片服务 + 登录
├── db.py              【数据库层】建库、存/读笔记、校验账号
├── guide_full.html    【攻略正文】你看到的全部装修内容都在这一个文件里
├── login.html         【登录封面页】输入账号密码的那个页面
│
├── notes.db           【你的笔记数据库】所有笔记都存在这（自动生成，备份就备份它）
├── notes_imgs/        【你的笔记图片】你从剪贴板 Ctrl+V 贴进笔记的图，自动存这（自动生成）
│
├── imgs/              【攻略配图】27 张施工工艺图，攻略里展示的那些图在这
│
├── static/
│   ├── note.js        【笔记前端逻辑】分条、贴图、灯箱放大都是它实现
│   └── note.css       【笔记样式】笔记框、图片缩略图、灯箱样式
│
├── requirements.txt   【依赖清单】Flask / Werkzeug
├── deploy.sh          【一键部署脚本】装环境+建库+注册开机自启
├── renovation_notes.service  【开机自启服务】给 systemd 用的
└── nginx_renovation.conf      【可选】想上 HTTPS 才用
```

---

## 二、图片放哪？（重点）

这个项目有**两类图片**，别搞混：

| 目录 | 是什么图 | 谁来放 | 怎么放 |
|------|----------|--------|--------|
| `imgs/` | 攻略里自带的施工工艺图（27 张） | 开发者/改攻略的人 | 把图文件丢进这个文件夹，再用代码引用；**一般你不用动** |
| `notes_imgs/` | **你自己**从剪贴板粘贴进笔记的图 | 你（在网页里操作） | 在笔记框里截图后 **Ctrl/⌘ + V** 粘贴，自动存这里，**不用手动管** |
| `notes.db` | 笔记文字 + 图片引用 + 链接 | 系统 | 自动维护，**备份就备份它** |

> 简单记：**你想往笔记里加图 → 网页里粘贴就行，图自动进 `notes_imgs/`；攻略配图在 `imgs/`，你基本不用碰。**

---

## 三、配置文件 `config.py` 逐项说明

打开 `config.py`（纯文本，用记事本/VS Code 就能改），每一项都带中文注释：

```
SITE_TITLE     = "装修防宰攻略 · 笔记版"   # 浏览器标签和登录页显示的站名
BIND          = "::"                      # 监听地址：:: = IPv6 双栈(IPv4/IPv6 都能进)，0.0.0.0 = 仅 IPv4
PORT          = 5000                      # 访问端口（浏览器输 域名:5000）
SECRET_KEY    = "renovation-notes-secret-2026"  # 会话签名密钥，公网建议改成你自己的长随机串
SESSION_DAYS  = 30                        # 登录后多少天免重登

USERNAME      = "phantomxjc"              # 登录账号（仅第一次建库写入）
PASSWORD      = "phantomxjc123"           # 登录密码（仅第一次建库写入）

NOTE_IMG_MAX_MB = 8                       # 单张笔记图片最大 8MB
ALLOWED_IMG_EXT = ("png","jpg","gif","webp")  # 允许的图片格式
```

**改完 `config.py` 一定要重启服务才生效**（服务器上）：
```bash
sudo systemctl restart renovation_notes
```

---

## 四、常见修改怎么做

### 1）改登录密码（最常用的需求）
`config.py` 里的 `USERNAME/PASSWORD` **只在第一次建库时生效**，之后改这里没用。正确做法二选一：

- **办法 A（最简单）**：登进服务器，用 Python 改数据库：
  ```bash
  cd /usr/local/renovation_notes
  ./venv/bin/python -c "import db; db.set_password('phantomxjc','你的新密码')"
  ```
  > 提示：当前 `db.py` 没有 `set_password` 函数，需要我加；或者直接告诉我，我给你一条能跑的命令。

- **办法 B**：删掉 `notes.db` 重建（⚠️ 会清空所有笔记，不推荐）：
  ```bash
  cd /usr/local/renovation_notes
  rm -f notes.db
  ./venv/bin/python -c "import db; db.init_db()"
  sudo systemctl restart renovation_notes
  ```

### 2）换端口 / 改监听方式
改 `config.py` 的 `PORT` 或 `BIND`，重启。或临时用环境变量：
```bash
PORT=8080 sudo systemctl restart renovation_notes
# 仅 IPv4：BIND=0.0.0.0 PORT=8080 sudo systemctl restart renovation_notes
```

### 3）换攻略内容
你新生成了 `guide_full.html` → 覆盖到项目根目录同名文件 → 重启服务（笔记代码和数据库都不用动）：
```bash
sudo systemctl restart renovation_notes
```

### 4）改界面样式
笔记框的样式在 `static/note.css`，攻略正文样式在 `guide_full.html` 内部的 `<style>`。改完重启 + 浏览器硬刷新（Ctrl+F5）。

### 5）笔记里粘贴的图片「双击放大」
已经做好：在任意一条笔记的图片上**双击**（手机上单击）就能全屏放大查看，再点一下 / 按 Esc 关闭。

---

## 五、部署 & 更新

### 第一次部署（服务器上）
```bash
cd /usr/local
sudo tar -xzf /path/to/renovation_notes_deploy.tar.gz --overwrite
sudo systemctl restart renovation_notes
```
浏览器打开 `http://你的域名:5000`，输入账号密码即可。

### 之后更新（比如我给了新包）
解压覆盖 + 重启，同上。**`notes.db` 和 `notes_imgs/` 不会被包覆盖，你的笔记不丢。**

### 备份（重要）
```bash
# 把笔记数据库和笔记图片一起打包带走
tar -czf my_notes_backup.tar.gz notes.db notes_imgs/
```

---

## 六、排错速查

| 现象 | 先试 |
|------|------|
| 打不开 / 超时 | 服务器本地 `curl http://127.0.0.1:5000` 看是否 200；查防火墙是否放行端口（IPv6 别漏） |
| 登录框一直弹 / 登不进 | 账号密码是否 `config.py` 里那组；`notes.db` 里 users 表是否有该账号 |
| 改了 config 没生效 | 记不记得 `sudo systemctl restart renovation_notes` |
| 图片不显示 | 浏览器 Ctrl+F5 硬刷新；确认 `notes_imgs/` 目录存在且有图 |
| 想看运行日志 | `sudo journalctl -u renovation_notes -n 50 --no-pager` |
